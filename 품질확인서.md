# 품질확인서 — jeonse-pnu (v0.4.0)

작성일: 2026-06-30 · 검증 환경: Python 3.12.3
대상: **연립·다세대(VWorld API) + 오피스텔(국세청 파일 DB) 통합 파이프라인**

---

## 1. 서비스 범위 확정 (HUG 126% 룰)

전세보증 한도는 시세 없는 주택에서 **공시가격 × 적용비율(126%)**로 산정된다.
따라서 본 서비스 대상은 **시세가 없는 물건만**:

| 물건 | 한도 산정 | 데이터 소스 | 본 서비스 |
|---|---|---|---|
| 아파트 | KB시세 기반 | (공시가 안 씀) | 제외 |
| 연립·다세대(빌라) | 공시가격×126% | VWorld 공동주택가격 API | 포함 |
| 오피스텔 | 기준시가×126% | 국세청 기준시가 파일 | 포함 |

공동주택구분코드: 1=아파트(제외), 3=연립, 5=다세대 (정의서 R25 확정).

## 2. 통합 구조 (두 경로)

```
등기부주소 → 코어(juso 정제 → PNU 조립)
  → 두 경로 동시 조회 후 값이 나오는 쪽 채택:
     ├ 연립·다세대 → VWorld getApartHousingPriceAttr (실시간 API)
     └ 오피스텔     → 국세청 기준시가 DB (build_officetel_db.py 적재)
  → 구·신 공시가 + 물건종류 + 신뢰도
```

한 물건은 둘 중 하나에만 존재하므로 종류 판별 없이 둘 다 조회해 안전하게 채택.

## 3. 데이터로 확정한 사실 (전부 실파일 검증)

### 연립·다세대 (VWorld API)
- 공식 명세 확정: getApartHousingPriceAttr, 입력 pnu/stdrYear/dongNm/hoNm/key/domain
- 출력 19필드, pblntfPc=공시가격. 사용자 제공 실응답(상암월드컵1단지 6,000만원)으로 파싱 100% 검증
- PNU 단지 중복 → PNU+동+호 매칭. 다세대 동명 공백 → 호명만 매칭

### 오피스텔 (국세청 파일)
- 2025·2026 기준시가 xlsx 각 ~249만 행(오피스텔 ~133만). EUC-KR, 15컬럼
- 핵심 발견: 파일의 '호'(0005)는 지번 부번이고, 실제 호수는 '상가건물호주소'(201) 컬럼
- PNU = 법정동코드+산여부+번지(본번)+호(부번). 호 매칭키 = 호주소
- 부천 법정동코드는 41190 아닌 41192/41194/41196(구 단위) — 실 법정동 자료로 확인

## 4. 요구사항 ↔ 구현 ↔ 검증 (v0.4 추가분)

| # | 요구사항 | 구현 | 검증 | 결과 |
|---|---|---|---|---|
| R30 | 오피스텔 PNU+호 조회 | officetel.fetch_officetel_by_pnu | 실DB 인터시티 201호 | OK |
| R31 | 오피스텔 구·신 2개년 | officetel.fetch_two_years | 201호 2,084,000→2,005,000 | OK |
| R32 | 두 경로 통합·자동선택 | pipeline 4-a/4-b | test_pipeline_officetel_integration | OK |
| R33 | 물건종류/건물명 출력 | LookupResult.property_type | 오피스텔 자동판정 | OK |
| R34 | 구년도 없을 때 현재값만 | pipeline elif 분기 | 경고+ok 처리 | OK |
| R35 | 전국 적재 스크립트 | build_officetel_db.py | 실파일 47,415행 적재→조회 | OK |

## 5. 검증 절차 결과 (4단계 전부 통과)

1. syntax(ast.parse) — 전 파일 통과
2. import — 성공, 공개 API 30개
3. 테스트 — 코어 12/12 + 파이프라인 8/8 = 20/20 통과
4. 정적분석(pyflakes) — 0건 클린

실DB 통합 검증: 등기부주소 "강서 화곡동 343-32 인터시티오피스텔 201호" 입력 →
juso 정제 → PNU 1150010300103430032 → 공동주택 API 빈 응답 → 오피스텔 경로 자동선택 →
구(2,084,000)·신(2,005,000) 기준시가 → 변동 -79,000원(-3.8%) → 신뢰도 B(83), 경고 0.

적재 스크립트 실행 검증: build_officetel_db.py로 실파일 47,415행 적재 후
조회 모듈로 201호 2,005,000원 정확 재현.

## 6. 한계 및 다음 단계 (정직한 고지)

- 라이브 키 호출 미실시: 연립·다세대 VWorld 실호출은 키+domain 등록 후 배포환경에서 1회 확인 필요.
- 오피스텔 적재: 검증은 타깃 3지역(강서/양천/부천)만. 전국 133만 행은 사용자 환경에서
  build_officetel_db.py를 --region 없이 실행해 적재(EC2/로컬 권장, Vercel은 관리형 DB 주입).
- 오피스텔 갱신: 국세청 파일은 연 1회. 매년 새 xlsx로 같은 스크립트 재적재.
  (연립·다세대는 API라 자동 갱신, 적재 불필요 — 비대칭 운영 주의)
- 한도 계산(126%·차감역산)은 범위 밖. 본 파이프라인 출력 = 구·신 공시가까지.

결론: v0.4.0은 HUG 126% 룰 대상(연립·다세대+오피스텔)을 모두 커버하는 통합
파이프라인을 완성했다. 연립·다세대는 VWorld 실시간 API, 오피스텔은 국세청 파일 DB로
이원화하되 단일 lookup()으로 통합했고, 20/20 테스트·4단계 검증·실데이터 대조를 통과했다.
남은 것은 라이브 키 호출 1회와 전국 오피스텔 적재(사용자 환경)뿐이다.

---

# 추가 (v0.5.0) — 오피스텔 Turso(libSQL) 운영 연동

작성일: 2026-06-30

## 배경
Vercel 서버리스는 로컬 SQLite 파일을 호스팅하지 못한다(함수가 ephemeral).
따라서 오피스텔 DB는 운영에서 **Turso(libSQL)** 로 조회하도록 이원화했다.
연립·다세대는 VWorld 실시간 API라 DB 자체가 불필요(비대칭 유지).

## 구현
- officetel.py: sqlite3 + libSQL 자동 판별
  - 우선순위 TURSO_DATABASE_URL(+TOKEN) > OFFICETEL_DB_PATH
  - 조회 결과를 dict로 정규화해 두 백엔드 동일 처리
- scripts/push_to_turso.py: 로컬 officetel.db → Turso 배치 업로드(libsql_client.batch)
- requirements.txt: libsql-client>=0.3.1

## 검증 (Turso 네트워크 차단 환경에서의 등가 검증)
컨테이너에서 turso.tech 직접 연결은 차단되나, libsql-client는 `file:` URL로
**동일한 API**를 사용하므로 전 경로를 등가 검증:
- libSQL 조회 경로: file: URL 연결 → fetch_officetel_by_pnu(conn=client)
  → 인터시티오피스텔 201호 2,005,000원 정확 (R36)
- push_to_turso 업로드: 현대드림타워 901호 적재→업로드→libSQL 조회 값 일치(2,341,000원) (R37)
- 회귀: sqlite 경로(db_path/주입 커넥션) 기존 동작 보존 확인

실제 Turso는 URL만 libsql://...turso.io 로 바꾸면 동일 동작(API 동일).

## 검증 절차 결과
- syntax/import OK
- 테스트: 코어 12/12 + 파이프라인 9/9 = **21/21 통과** (libSQL 케이스 추가)
- pyflakes 0건 클린
- 프론트 vite build 성공(35모듈)

## 남은 것 (사용자 환경)
1. Turso DB 생성(turso.tech 또는 Vercel Marketplace Turso Cloud 연동)
2. 로컬 적재 → push_to_turso.py 로 업로드(전국 133만 행)
3. Vercel 환경변수: JUSO/VWORLD/VWORLD_DOMAIN/TURSO_DATABASE_URL/TURSO_AUTH_TOKEN
4. VWorld 라이브 키 호출 1회 확인(연립·다세대)

---

# 추가 (v0.6.0) — 주소정제 설계 방법론 반영 (캐스케이드 + 동명이지)

작성일: 2026-06-30

## 배경
과거 "주소 정제 기술" 설계(juso 퍼지매칭을 정제엔진으로 + 캐스케이드 사다리 +
동명이지 후보 정책)를 코드가 절반만 반영하고 있었음. 다중매칭 시 "1순위 사용"으로
단정해 엉뚱한 PNU 조립 위험이 있었음. 설계대로 전면 보강.

## 반영 내용
1. 동명이지(同名異地): juso 후보를 법정동 prefix(PNU 10자리)로 그룹핑.
   - 여러 행정구역 매칭 → ambiguous=True + region_candidates 전부 반환(단정 금지)
   - 같은 법정동 복수표현 → prefix로 압축해 자동 확정
2. 캐스케이드 폴백 사다리:
   - ① 원문 juso → ② 동/호 제거본 juso → ③ 카카오 키워드 → ④ 점진적 절삭
3. 파이프라인/refine 엔드포인트에 ambiguous 정직 전파(상위 행정구역 요구)

## 검증 (실 juso 응답 형태로)
- 동명이지: "중앙동 300"→부산/대전/포항 3곳 후보 전부 반환, 1순위 단정 안 함 (test_geocode_dongmyeong_iji)
- 압축: "역삼동" 같은 법정동 복수표현 → 1곳 자동 확정 (test_geocode_same_dong_compressed)
- 캐스케이드: 동/호 붙은 원문 0건 → 제거본/절삭으로 성공 (test_geocode_cascade_strip_detail)

## 검증 절차 결과
- 코어 12/12 + 파이프라인 12/12 = 24/24 통과
- pyflakes 클린, 프론트 빌드 성공

## 남은 것
- 점진적 절삭은 최대 2토큰까지(과도한 절삭 방지). 운영 데이터로 임계 조정 가능.
- 프론트에 동명이지 후보 선택 UI(시/도 재입력)는 백엔드 region_candidates를 받아 추가 예정.

---

# 추가 (v0.6.1) — 10에이전트 × 86시나리오 리허설 + 버그 수정

작성일: 2026-06-30

## 리허설 결과: 86/86 전체 통과 (결함 0)
10개 관점 에이전트로 다양한 시나리오를 실제 코드에 실행:
A1정제12 A2동명이지8 A3캐스케이드8 A4PNU12 A5오피스텔9 A6에러9 A7보안8 A8경계10 A9라우팅8 A10회귀2

## 발견·수정한 진짜 버그 (1건)
officetel.fetch_officetel_by_pnu: 호를 명시했는데 매칭 실패 시 첫 세대값(units[0])으로
폴백해 ok=True로 잘못된 가격을 반환하던 문제. (예: 없는 '9999호'가 201호 값으로 성공)
→ 호 명시 시 정확 매칭만 인정, 미매칭이면 정직하게 실패(ok=False)하도록 수정.
   호 미지정 시에만 대표 세대값 사용(주의 경고). "추측은 쓰레기" 원칙 준수.
재검증: 없는호=실패, 있는호 201=2,005,000원 정상.

## 오탐(테스트 측 오류, 코드 정상)
- PNU 본번 9999 → 1111010100199990000 정상(본번 4자리 zfill). 테스트 기대값 오기였음.
- 동명이지 6곳/절삭 mock 조건 불일치. 코드 정상.

## 검증 절차
- 코어 12/12 + 파이프라인 12/12 회귀 통과(버그 수정 후)
- pyflakes 클린, 프론트 빌드 성공

---

# 추가 (v0.6.2) — VWorld 정밀 진단 강화 (개발키/도메인 격리)

작성일: 2026-07-03

## 분석 (진단 결과로 확정된 사실)
- 사용자 VWorld 키(C8FB…): 활용API에 국가중점/2D데이터 모두 체크됨(활용신청 완료),
  요청주소도 ned/data/getApartHousingPriceAttr로 우리 코드와 일치.
- 그럼에도 INCORRECT_KEY. 단, 주소검색(req/address)은 통과.
- 키 상태: **개발키**(개발 환경). 서비스URL=ltvcheck.vercel.app 등록됨.
→ 원인 후보: 개발키의 서버호출 제약 또는 domain 파라미터 불일치.
  (앞서 "활용신청 안 됨"으로 본 진단은 오류였음 — 사용자 화면으로 정정)

## 설계
vworld-diag가 공동주택가격 API를 domain 3종(없이 / vercel.app / https://vercel.app)으로
실제 호출해 통과 조합을 실측. 통과 조합이 있으면 그 VWORLD_DOMAIN 값을 추천,
전부 실패면 개발키→운영키 전환을 안내.

## 검증
- 시나리오A(domain=vercel.app 통과): 판정이 정확히 그 값 추천 ✔
- 시나리오B(전부 INCORRECT): 운영키 신청 안내 ✔
- syntax/pyflakes 클린, 회귀 24/24 유지.

## 요구사항 적합성
목표(INCORRECT_KEY 원인 격리 및 해결경로 제시) 충족. 추측 없이 키가 직접 답하도록 설계.

## 남은 것 (사용자 액션)
1. /api/vworld-diag 재배포 후 실행 → conclusion 확인
2. 통과 domain 값이 나오면 VWORLD_DOMAIN 설정 / 없으면 운영키 신청

---

# 추가 (v0.6.3) — 물건종류 오판정 수정 (아파트 대상외 처리)

작성일: 2026-07-03

## 분석 (라이브 화면에서 발견한 결함)
VWorld 연동 성공 후 "센트레빌레우스1단지" 조회 시:
- 물건종류가 "연립·다세대"로 잘못 표시 (실제는 아파트)
- 원인: pipeline이 VWorld 값이 나오면 무조건 property_type="공동주택"으로 박고,
  프론트가 오피스텔 아니면 전부 "연립·다세대"로 하드코딩.
  → VWorld 응답의 aphusSeCodeNm(아파트/연립/다세대)을 안 읽었음.
- 서비스 대상 규칙 위반: 아파트는 KB시세 기반이라 공시가×126% 대상이 아님(초기 확정사항).

## 설계
1. pipeline: 채택 세대의 aphusSeCodeNm으로 property_type 정확 판정
2. 아파트면 is_target=False + "대상 아님" 경고
3. 프론트: 실제 종류 표시, 대상외(아파트)면 값 대신 "이 서비스 대상이 아니에요" 안내

## 개발
- pipeline.py: aphusSeCodeNm 판정 + is_target 필드 추가
- LookupResult에 is_target(기본 True), to_dict(asdict)로 자동 노출
- App.jsx: adaptApiResult가 실제 종류 사용 + isTarget 전달, 대상외면 StatusCard로 안내

## 검증
- 아파트 응답 → property_type="아파트", is_target=False, 대상외 경고 ✔
- 다세대 응답 → property_type="다세대", is_target=True 정상 대상 ✔
- 코어 12/12 + 파이프라인 12/12 회귀 통과, pyflakes 클린, 프론트 빌드 성공

## 요구사항 적합성
"연립·다세대·오피스텔만 대상, 아파트 제외" 규칙을 실제 데이터 기반으로 준수하도록 수정.
물건종류를 하드코딩이 아니라 VWorld 실제 구분값으로 판정 → 적합.

## 남은 이슈 (별도)
- 동·호 없는 대단지 조회 시 신뢰도 F로 뜨는 값의 세대 특정 문제(대표세대값). 
  → 동·호 입력 유도 UX 또는 F등급 시 값 표시 억제는 추후 논의.

---

# 추가 (v0.6.4) — 아파트: 차단이 아니라 "공시가 표시 + 시세기반 고지"

작성일: 2026-07-03

## 피드백 반영
v0.6.3에서 아파트를 "대상 아님"으로 값까지 차단한 것은 과했음.
공시가격은 존재하므로 보여줘야 함. "한도 계산 대상 아님" ≠ "공시가 미표시".

## 재설계
- 아파트도 구·신 공시가 + 증감을 정상 표시(차단 제거)
- 안내 문구만 종류별 분기:
  - 연립/다세대/오피스텔: "보증 범위가 늘/줄 수 있어요"
  - 아파트: "아파트는 공시가로 한도를 정하지 않아요 / KB시세 기준. 이 변동이 한도에 그대로 반영되진 않아요"
- 타입 배지에 아파트는 "· 참고용" 표기

## 개발
- App.jsx: 대상외여도 reveal로 값 표시(차단 로직 제거)
- Result.jsx: isApt 분기 — 값은 동일, guide 문구/아이콘/배지만 변경

## 검증
- 아파트: 값 표시 O(구442,000,000 신459,000,000), 안내=시세기반 고지 ✔
- 연립/다세대/오피스텔: 기존 보증 안내 유지 ✔
- 코어 12/12 + 파이프라인 12/12, pyflakes 클린, 프론트 빌드 성공

## 요구사항 적합성
"아파트는 계산 대상 아니어도 공시가는 보여줘야 함" 피드백을 정확히 반영.
정보는 제공하되 오해 없도록 한도 산정 방식 차이를 명확히 고지 → 적합.

---

# 추가 (v0.7.0) — 임의 대표세대 제거, 동·호 필수화

작성일: 2026-07-03

## 분석 (요구/문제)
같은 PNU에 세대가 여럿(아파트·대단지)이면 세대별 공시가가 다름.
기존 코드는 동·호 미특정 시 units[0](첫 세대) 등 임의 대표값을 사용 → 틀린 값을 표시.
"추측은 쓰레기" 원칙 위반. 동·호가 필요한 곳은 필수로, 특정 못하면 값을 만들지 않아야 함.

## 설계
값 확정 규칙(임의 대표세대 금지):
- 매칭된 세대 있으면 그 값
- 매칭 없고 세대가 정확히 1건이면 그 값(특정 불필요)
- 세대 여럿 + 미특정 → 값 안 냄, needs_unit=True → 동·호 입력 요구
아파트만이 아니라 "여러 세대 반환" 모든 경우에 적용(종류 무관).

## 개발
- gongsiga.PriceResult에 needs_unit 추가. 폴백(res.units[0]) 제거.
- pipeline: apt_cur.needs_unit 분기 신설 — 값 없이 종류·건물명·needs_unit만 채워 조기 반환.
- LookupResult.needs_unit 추가(asdict로 노출).
- App.jsx: needs_unit이면 "동·호를 입력해 주세요" 안내 + 동·호 입력칸 강조(.need),
  placeholder를 예시/필수로 변경.

## 검증 (실 응답 형태)
- 여러 세대 + 동·호 없음 → ok=False, price=None, needs_unit=True (임의값 안 냄) ✔
- 여러 세대 + 호 매칭 → 해당 세대값(461,000,000) ✔
- 세대 1건 → 동·호 없어도 확정 ✔
- 회귀: 코어 12/12 + 파이프라인 13/13, pyflakes 클린, 빌드 성공

## 요구사항 적합성
"동·호 필요한 곳 필수, 임의 대표세대 금지" 요구를 코어~프론트 전 구간에 반영.
정확도 우선(틀린 값보다 '입력 요구')로 설계 → 적합.
