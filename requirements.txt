# api/lookup.py (운영 함수)는 표준 라이브러리 + DB 드라이버만 사용.
# 연립·다세대(VWorld API)는 외부 의존성 0.
# 오피스텔 조회 DB 드라이버(택1, 환경에 맞게 자동 선택):
#   - Vercel Postgres(Neon): psycopg
#   - Turso: libsql-client
psycopg[binary]>=3.1
libsql-client>=0.3.1
